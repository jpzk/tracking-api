machine:
  java:
    version: openjdk7

dependencies:
  cache_directories:
    - "~/.ivy2"
  override:
    - wget https://dl.bintray.com/sbt/native-packages/sbt/0.13.11/sbt-0.13.11.zip:
        pwd: "~"
    - unzip sbt-0.13.11:
        pwd: "~"


test:
  override:
    - ~/sbt/bin/sbt test

